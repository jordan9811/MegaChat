# Needs a status call

Everything the docs pass had to mark `UNCLEAR`: the sources disagree, or the repo does not say. Each is a decision only the owner can make. When one is decided, change the tag on the page named, and remove the row.

| # | Question | Where the sources disagree | Page that carries the tag |
|---|---|---|---|
| U1 | **Is `/api/join/mpp` a live seat-payment mode or retained code?** | `server.js` mounts `/api/join/mpp` and `/api/meter/tick` (TIP-1034 channels, `meter-mpp.js`) beside `/api/join/passkey`; `web/lib/join-page.ts` calls only `/api/join/passkey` for both wallet kinds ("ONE metered join flow for both modes"). The retired Gateway route says so in its 501 body; the MPP route says nothing either way. If it is live, something other than the join page must reach it; if retained, the retest checklist should not carry it as a money path. | `concepts/money-and-metering.md`, `features/live-seats.md`, `internal/launch-readiness.md` row 2 |
| U2 | **Kick VOD wording.** Is "Kick has no VOD we can read" (what streamers are shown) the intended claim, or the corrected one? | `bounty-claim.config.js` `PLATFORM_PROFILES.kick.notice` says Kick has no VOD we can read. `OPEN-ISSUES.md` "Terminology correction" (2026-08-29) records that Kick keeps VODs and only *discovery* is missing, with a one-line correct form ready for reuse. The copy was not changed after the correction. | `features/platform-support.md` |
| U3 | **Is dark mode still broken?** | `_gate-theme.mjs` reported `GATE FAIL (3)` on 2026-08-26 — dark landing, dashboard and join rendered a white background (`OPEN-ISSUES.md` T1). The Nerve skin (`e92eae7`, 2026-09-04) made the site near-black everywhere. Nothing records the gate being re-run since. | `internal/work-history/2026-08-29-09-06-ui-overhaul.md`, `internal/outstanding.md` E21 |
| U4 | **Should the bounty badge follow the layout origin, or should the layout editor refuse `bottom-left`?** | `ROADMAP.md` specced "the barcode takes the diagonally opposite corner automatically"; Pass A shipped the editor with the badge still fixed at bottom-left (`public/overlay.html`). Either fix closes L13; they are different products for the streamer. | `internal/limitations-register.md` L13, `internal/outstanding.md` E1, `internal/roadmap.md` |
| U5 | **Does the owner's Notes group belong in the public half?** | `megachat-pass-b-and-docs.md` says `--public-only` drops the Internal group and says nothing about `docs/notes/`. This pass drops Notes too, on the reasoning that free-form scratch should not publish by default (`scripts/docs-summary.mjs`). One line to flip. | `internal/README.md`, `REPORT-DOCS.md` Gate 0 |
| U6 | **Viewer-proportional payout: dead or deferred?** | `OPEN-ISSUES.md` B6 (2026-07-26) specifies payout scaling proportionally to viewers and asks for viewer samples to be captured so it can be built later. `bounty-stream-context.js` (2026-07-29) argues against any viewer weighting and a gate asserts its absence. The samples are captured; the weighting is refused. The roadmap lists it as `IDEA` with the code's position noted. | `internal/roadmap.md` |
