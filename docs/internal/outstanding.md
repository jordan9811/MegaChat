# Outstanding

Every known-broken item, open decision and deferred thing, reconciled against `OPEN-ISSUES.md` entry by entry (every `### ` heading and every "open" bullet there was read for this page). Each row says where it is blocked and what would unblock it. Items that `OPEN-ISSUES.md` records as RESOLVED, RETRACTED or SUPERSEDED are not repeated here; the [work history](work-history/README.md) carries them.

Blocked-on codes: **owner** (a credential, an account, a dashboard toggle, a decision), **broadcast** (needs a real stream with the operator present), **engineering** (nothing external needed), **decision** (a product call).

## On the owner

| Id | Item | Where it is blocked | What unblocks it | Source |
|---|---|---|---|---|
| O1 | **CLOSED 2026-09-16 (Pass C).** Run against the production volume over `railway ssh`, report mode then `--write`: both printed "Nothing to migrate — no guest list at /data/guest-whitelist.json". The store has never been written on prod, so nothing was pinned and nothing was unresolvable; the volume was byte-identical afterwards. Any entry added from now on pins `identityKey` at add time. | — | — | `_migrate-whitelist-identity.mjs`; [Guest whitelist](../features/guest-whitelist.md) |
| O2 | **CLOSED — verified 2026-09-16 (Pass C).** `BOUNTY_ADMIN_KEY` IS set on the production service (`railway variables`, names only). `OPEN-ISSUES.md` still records it as unset, which was true when written and is not now. | — | — | `OPEN-ISSUES.md` (stale); Railway service variables |
| O3 | Configure LiveKit Cloud webhooks (`participant_joined`, `participant_left` → `/api/livekit/webhook`); the burn breaker meters zero and is inert without them | owner | Cloud dashboard step | `OPEN-ISSUES.md` L3 (2026-07-25); real delivery was verified once on 2026-07-26 — confirm the setting still holds |
| O4 | Confirm Railway's app-sleep toggle is OFF; a slept container drops `participant_left` and leaves sessions open in the breaker's ledger | owner | dashboard check | `OPEN-ISSUES.md` L7 |
| O5 | Paste a real `OPS_ALERT_WEBHOOK` and fire `POST /api/livekit/burn/test-alert` | owner | one variable, one request | `OPEN-ISSUES.md`, 07-27 "Owner-blocked" |
| O6 | Decide the sub-3 s residual (options written) | decision | the owner's call | `docs/decisions/sub3s-residual.md`; `OPEN-ISSUES.md` P2 |
| O7 | Decide whether MegaChat wants coin-keyed (pump.fun mint) payouts at all | decision | product call | `OPEN-ISSUES.md`, "pump.fun ownership is unsolved" (3) |
| O8 | Decide the two `.claude/worktrees/` scratch branches (one commit ahead each), and the stash `f7219f416b99e59e9188b1cf1e9a988fa6e6fc73` that is not mine | owner | inspect, then keep or drop | `OPEN-ISSUES.md`, "STASHED EDITS" |
| O9 | The bounty admin `/bounty/admin` and its policy table are behind the flag; before any public flag-on, O2 and the retest checklist | owner | [Launch readiness](launch-readiness.md) | — |
| O10 | **ALREADY FLIPPED — verified 2026-09-16 (Pass C):** production runs `OBS_ONECLICK=1`. What remains is the checklist it was meant to wait for: rows 8–14 of `docs/obs-oneclick-checklist.md` still need a real OBS on the owner's machine, and B1 below is the same gap. | owner | `node _verify-obs-oneclick.mjs` rows 8–14 | Railway service variables; `OPEN-ISSUES.md` (stale) |
| O11 | **CLOSED — verified 2026-09-16 (Pass C).** Both `MODERATION_API_KEY` and `CONTACT_URL` are set on the production service. `OPEN-ISSUES.md` still lists them as needed. | — | — | `OPEN-ISSUES.md` (stale); Railway service variables |
| O12 | Two stale worktree registrations (`create-room-ui`, `mc-guest-whitelist`) print `Permission denied` on every commit because OneDrive holds the files | owner | pause OneDrive sync for the folder, then `git worktree prune` | Pass A report; `REPORT-DOCS.md` |
| O13 | **The bounty program is LIVE on production** — `BOUNTY_CLAIM=1` on the service, so every `/api/bounty/*` route is mounted and `/bounty` renders the board. The handbook and the launch-readiness page describe it as off-by-default; that is the code default, not the deployed state. Nothing has run through it (no evidence chain, no captures on the volume) and settlement is still a stub, so no money can move — but the surface is reachable by anyone who finds it. Decide whether it should be on in stealth. | decision | one variable | Railway service variables; `bounty-claim.config.js` |
| O14 | **An SSH key now exists for the production container.** Pass C created `~/.ssh/railway_megachat_ed25519` (a scoped key, not your default identity) and registered its public half with your Railway account as `claude-passc-megachat-migration`, because `railway ssh` is the only sanctioned path to the volume and O1 required it. It is how the launch-readiness retest will reach prod data. Remove it with `railway ssh keys remove claude-passc-megachat-migration` and `rm ~/.ssh/railway_megachat_ed25519*` if you would rather it not exist. | decision | your call | Pass C Session 1 report |
| O15 | **Who pays the streamer for credit- and points-funded seats?** The viewer's credit lives in the reward pool; the platform wallet never receives it, so E38 retains every intent for those seats rather than invent a payer. Options: the reward pool pays the streamer's share (signer `rewardPool` at the door — a one-line change), the platform funds it, or those seats stay unpaid by design. | decision | your call | `settlement.js` (E43); register L39 |
| O16 | **Small rooms cannot escalate.** With three distinct flaggers required, a one- or two-viewer session never extends its hold. Acceptable for launch (the streamer refund covers the honest case; escalation only delays), or close it with unanimity when a session has fewer than `minFlaggers` depositors — four lines, the depositor count is already tracked. | decision | your call | `docs/internal/escrow-contract.md`, the small-room question |
| O17 | **CLOSED 2026-09-19.** Set on the service as `ESCROW_OPERATOR_KEY` (funded 3.00 USDC.e by the owner) and `ESCROW_ATTEST_KEY` (funded 0.10 by `scripts/fund-escrow-roles.mjs`), both stored without the `0x` prefix, which the loader accepts. | — | — | `escrow-chain.js`, `normalizeKey` |
| O18 | **The classifier's thresholds, and what any of it should unlock.** The numbers are reasoned guesses with no users behind them (register L48), and no feature reads them. Before one does: are these the right bars, and what does each tier get — zero friction, a prompt, a refusal? | decision | your call | `classifier.js`, `THRESHOLDS`; `docs/internal/account-model.md` |

## Needs a real broadcast

| Id | Item | What would settle it | Source |
|---|---|---|---|
| B1 | obs-websocket path against a real OBS during a real broadcast, with verification reading the result | operator live with OBS, `--skip-push`, `_smoke-obs-overlay.mjs` | `OPEN-ISSUES.md`, "The obs-websocket path is still untested against real OBS"; R8 |
| B2 | Kick re-measured on the 75 s / 51 s freeze window | a Kick rehearsal (`_rehearsal-kick.mjs`) — needs `KICK_STREAM_KEY`, `KICK_RTMP_URL` locally | `OPEN-ISSUES.md`, "KICK IS STILL UNPROVEN" → run #5 PASS predates `b3ccf8b` |
| B3 | Twitch "every SECOND code of a window misses" (T5b) — likely subsumed by the overlay polling fix, **to be confirmed, not assumed** | a fresh Twitch broadcast with the refreshed overlay; the 2026-08-30 run (det 1.000) is consistent with it being fixed | `OPEN-ISSUES.md` T5b, "ROOT CAUSE FOUND" |
| B4 | Does pump.fun's playlist outlive the broadcast? Verification runs after the fact | measure after a stream ends | `OPEN-ISSUES.md` T8 |
| B5 | pump.fun self-capture vs external on one broadcast, after the coin-page fix (both columns were the same capture on 08-29; genuinely different on 08-30 — confirm it holds) | re-run `_rehearsal-pumpfun.mjs` | `OPEN-ISSUES.md`, pump.fun P2, "BOTH READ PATHS PROVEN" |
| B6 | Rumble end to end — needs a fresh livestream slot and the watch URL from the address bar while live | operator | `OPEN-ISSUES.md` R5, "Rumble: ingest PROVEN" |
| B7 | `timelineSpreadMs` across several real broadcasts, to decide R1/R2/R3 | several broadcasts on any platform; the value is recorded on every verification now | `OPEN-ISSUES.md` R1–R3 |
| B8 | Kick calibration close to a reliability boundary — how often does an honest Kick streamer land in manual review? | operational tracking | `OPEN-ISSUES.md`, "golden loop" retest, Kick run #5 |

## Engineering, no external dependency

| Id | Item | Source |
|---|---|---|
| E1 | **CLOSED 2026-09-16 (Pass C).** The badge now takes the corner diagonally opposite where the stack actually lands, and a layout that would still reach it at the ten-tile ceiling is refused on the write path with a reason. `_gate-overlay-layout.mjs` 33/0 measures a real browser across all sixteen origin/direction combinations. | `public/overlay.html`, `applyBadgeAnchor`; `rooms-store.js`, `layoutCollision` |
| E33 | **The layout editor was inert — the overlay never received a layout at all.** CLOSED 2026-09-16 (Pass C): `server.js` sent `resolveRoomConfig(id)?.config?.layout`, and that function returns a flat object with no `config` key, so the value was always `undefined` and every room rendered the built-in default. The same expression fed the seat cap the literal 3. Admission was never affected. | `OPEN-ISSUES.md`, "THE LAYOUT EDITOR WAS INERT" |
| E34 | **`direction: left`/`right` was never implemented.** CLOSED 2026-09-16 (Pass C): `relayout()` only wrote `box.style.top`, so a horizontal stack rendered as a downward column while the editor previewed a row. | `OPEN-ISSUES.md`, "DIRECTION WAS NEVER IMPLEMENTED" |
| E35 | **Five browser gates assert against a UI that was replaced.** `_gate-polish`, `_gate-browse-deck`, `_gate-browse-thumb`, `_gate-cam-autoswitch` and (partly) `_gate-free-megachat` were last touched in July and assert on markup and copy the 2026-08-29 → 09-06 overhaul deliberately rebuilt — `#price`, the landing "equation" line, a zero-git-diff check against a pre-overhaul commit. They have been red and unwatched ever since, which is T2 with a cost attached. Re-pointing them is real work and deserves its own pass. **Until it happens, "the browser gates are green" is not a claim anyone can make, and the retest checklist depends on it.** | `OPEN-ISSUES.md`, "THE ELEVEN BROWSER GATES WERE NOT GRADING THE PAST" |
| E37 | **CLOSED 2026-09-17.** No path put an approved pledged clip on air the first time. The cause was three missing joins, not one: `markPlayed` had no caller, the air session was never bound to a room (the claim page called `startAirSession` without one, so every production session carried `roomId: null` and the letters hook could never match), and the claim page handed out the by-id overlay URL that the overlay's own source describes as rotting. First airings and replays now share one dispatcher, one rate limit and one bridge; a session always binds to a room; `markPlayed` is called where a playback starts. `_gate-first-airing.mjs` 31/0, including a real browser overlay rendering a clip that was only ever approved. | `OPEN-ISSUES.md`, 2026-09-17 |
| E41 | **CLOSED 2026-09-18 (E38): the gate clears the seeded board before it measures, 41/0.** Was: `_gate-bounty-program.mjs` red since 2026-09-01 — the demo board seeded on first boot adds 900 of escrow and the gate expected its own 110. Not a product defect; a stale expectation. | `OPEN-ISSUES.md`, 2026-09-17; `_gate-bounty-program.mjs` |
| E40 | **Queue ORDER is oldest-approved-first, and nobody has decided whether it should be the streamer's to change.** Skip, hold and reorder are unbuilt; the E37 pass made the simplest defensible choice rather than inventing a product surface. Worth deciding before launch: a streamer with a large queue and no control over it is a support problem. | `bounty-bank.js`, `firstAiringCandidates` |
| E38 | **CLOSED 2026-09-18.** The seat pending bucket is real money now: ticks pull into the platform wallet and every escrow intent goes through `settlement.js`, paid by the flush once `PLATFORM_SETTLEMENT_KEY` is set (unset on prod, so PENDING). Funded run: Part 4. | `settlement.js`; `_gate-money.mjs` |
| E39 | **MPP seats do not pause while the overlay is hidden.** Their ticks are client vouchers; refusing one trips the stale-kick and ends the seat. Only the server-driven meters skip ticks. Since E38 they DO feed the seat bucket (`seatEscrow.accrue` on every paid voucher), so their buried seconds are refunded; the pause itself is still open (L34). | `server.js`, `tickAllMeters`; register L34 |
| E36 | **`npm run lint` has never been runnable.** `web/package.json` runs `eslint .` and no ESLint config has ever been committed on any branch; ESLint 10 requires a flat config and exits 2. Three run prompts have now listed "lint" as a verification step. Add a config (choosing the rule set is a real decision) or drop the script. | `web/package.json`; `OPEN-ISSUES.md`, "npm run lint HAS NEVER BEEN RUNNABLE" |
| E42 | **CLOSED 2026-09-18.** A points-funded seat carried the room's USDC tick price in 6-decimal atomic units against a whole-point balance, so every points seat was kicked `out_of_funds` on its first tick and refunded. Found by building the L35 fixture. The credit join now passes point-denominated atomics. | `server.js`, `createSeat`; `_gate-meter-pause.mjs` |
| E43 | **CLOSED 2026-09-18.** Credit- and points-funded seats opened escrow buckets carrying the USDC address, so once the door existed their sweeps and refunds would have become real platform-wallet intents for money that never entered the platform wallet. Their buckets now carry no token address and the door RETAINS such intents. | `server.js`, `createSeat`; `settlement.js`; `_gate-money.mjs` T1 |
| E44 | **Two sweeps still open from the E38 audit:** Arc-era references outside the deleted page (comments and helpers that still say Arc), and testnet framing in prose that describes a mainnet app. Neither changes behaviour; both mislead the next reader. | `OPEN-ISSUES.md`, 2026-09-18 |
| E45 | **Five gates assert the deleted passkey bundle is served** — `_gate-phase1`, `_gate-phase2`, `_gate-dashboard-phase1`, `_gate-ui-part-a` (all four already red here for W5) and `_gate-auth` (re-pointed at the join page 2026-09-18). Re-point the four when they are ported to `startGateServer`. | `_gate-money.mjs` P (the 404 is now the asserted behaviour) |
| E46 | **The legacy Gateway seat mode has no client.** `paymentMode: 'gateway'` seats (`tickPrepaidSeat`) were deposited from the deleted /index.html; the server-side route and tick path remain. Retained code or a live mode — the same question as U1 for MPP. | `server.js`, `tickPrepaidSeat`; `_gate-money.mjs` P |
| E47 | **CLOSED 2026-09-19 (Session 2).** Mainnet deployment `0x432bb6c62503711ad4e70a91247a110e61779213`; deposit at join; the ledger's figures into `attest`; `tickPasskeyStreamSeat` pays nobody for an escrow seat; the direct fallback rewritten with the payout address as the only recipient; a room without one refused. `_gate-escrow-seat.mjs` 29/0 on mainnet dust. | `docs/internal/escrow-contract.md`, Session 2 |
| E48 | **Privy users — the wallet layer — still pay into MPP channels whose payee is the platform wallet.** `joinSeat` in `web/lib/join-page.ts` sends a Privy wallet to `/api/join/mpp` whenever `meterMode` is `mpp_session`, which production is; only MetaMask reaches the escrow. The scoping pass's claim that MPP was unreachable from the UI was wrong (a `head -5` on a grep). One client condition routes Privy through the metered join; whether to, or to point the channel payee at the streamer instead, is a decision. | `web/lib/join-page.ts`; register L44 |
| E49 | **CLOSED 2026-09-19 (Session 2).** Every door transfer since E38 would have failed on mainnet: the server's hand-rolled chain object dropped `feeToken`, writes went out as EIP-1559, and fees defaulted to pathUSD, which no wallet of ours holds. Found when the escrow deposit and then the fallback's first pull both died with `insufficient funds … have 0`. Every server signer is now on viem's Tempo chain and every transfer names its fee token. | `server.js`, `tempoViemChain`; `settlement.js`; register L47 |
| E50 | **Gate H should assert that every server-side `createWalletClient` is built on viem's Tempo chain**, so the next signer added on a bare chain object fails the gate instead of its first mainnet send. | `_gate-money.mjs`; register L47 |
| E51 | **Nothing consumes the classifier.** It is built, gated and shown to the account holder; rewards, gating and anything else that might read it are unwritten, and what each tier should unlock is undecided. | `classifier.js`; `docs/internal/account-model.md` |
| E52 | **Opacity and Reclaim are stubs.** Each file is the whole SDK boundary and returns `not-configured`. Wiring either is filling in one file — but Opacity's documentation site was returning a Cloudflare DNS error, so its contract could not be read from the source of truth. | `attestation/opacity.js`, `attestation/reclaim.js` |
| E53 | **A Privy-reported social cannot be upgraded automatically.** Privy gives a username but no platform id, so the only way to turn one into a real link with attributes is for the person to connect that platform through our own OAuth. Whether Privy's `linked_accounts` carries an id we could use was not investigated. | `privy-identity.js`; register L50 |
| E2 | A1: bind X's client-supplied self-capture URL to the X identity; the comment above `captureSourceUrl` saying those platforms "are not claimable yet" is false and load-bearing | `OPEN-ISSUES.md`, adversarial review A1 (high) |
| E3 | A2: one degraded Privy fetch permanently deletes `platformLogins` — the merge must be additive and an empty fetch must never be written | `OPEN-ISSUES.md` A2 (high); `privy-identity.js` |
| E4 | A3: no round-trip test for the widened `recordVerification` whitelist (`detectionRate`, the `timeline*` fields) — partially covered by `_gate-confidence-split.mjs` H after the regression came back once | `OPEN-ISSUES.md` A3; "golden loop" retest |
| E5 | A4: gate E4 asserts against its own copy of the pump.fun live-narrowing; export the real function | `OPEN-ISSUES.md` A4 (low) |
| E6 | `verifiedClips` counts scaffolding (canary, setup) windows — inflates the payout unit | `OPEN-ISSUES.md`, pump.fun P1; "STILL OPEN" under the dead-recorder entry |
| E7 | An open review keeps its original reason and never learns the newer one; reasons should append | `OPEN-ISSUES.md`, P1 (07-29) |
| E8 | `NO_VOD_COVERING_TS` reported for an offline channel in live mode — the wrong classifier branch matches first | `OPEN-ISSUES.md`, P1 (07-29) |
| E9 | Capture storage has no global ceiling across concurrent sessions | `OPEN-ISSUES.md`, repeated since 08-24 |
| E10 | pump.fun manifests (~320 kB, append-only) re-fetched every 2 s poll — size for it or poll slower before enabling the platform | `OPEN-ISSUES.md` P3 (08-25) |
| E11 | Placeholder-stream detection on pump.fun (T6) — the cheap, fail-safe option chosen, not built | `OPEN-ISSUES.md` T6 |
| E12 | Overlay renders "server unreachable" instead of blanking; setup helper prints a countdown (T7) | `OPEN-ISSUES.md` T7 |
| E13 | Log discarded calibration probes and cluster size on every calibration | `OPEN-ISSUES.md`, "Calibration once swallowed an injected DISAGREEMENT" |
| E14 | The stub calibration fixture cannot construct offsets ≥ ~30 s; rebuild by concatenating per-badge segments | `OPEN-ISSUES.md`, calibration P1 |
| E15 | `_rehearsal-run-b.mjs` exits with a libuv assertion after its report | `OPEN-ISSUES.md`, calibration P1 |
| E16 | Contributions with no uploaded clip are refundable (`CLIP_NEVER_UPLOADED`) but nothing sweeps them | `OPEN-ISSUES.md`, 07-27 "Still open" |
| E17 | The platform match has no reversal story; no post-release clawback for BOUNTY releases. **Partly reused 2026-09-16:** the designed holdback (option B) is now built for live seats (`seat-escrow.js`); bounty releases still pay in full with a bookkeeping-only dispute window. | `OPEN-ISSUES.md`, 07-27; `docs/decisions/post-release-clawback.md` |
| E18 | `_gate-mpp-clientpath.mjs` still mirrors the payment path (drift-checked, not converted) | `OPEN-ISSUES.md`, 07-27 |
| E19 | Port the fixed-sleep gates to `startGateServer` (list recorded in W5); lift G0 into the helper and adopt it in eleven browser gates | `OPEN-ISSUES.md` W5; 2026-09-16 |
| E20 | One runnable entry point for the gate suite, with isolation or retry (T2) | `OPEN-ISSUES.md` T2 |
| E21 | `_gate-theme.mjs` reported red for dark mode on 2026-08-26 (T1); not re-checked since the Nerve skin — [U3](needs-a-status-call.md) | `OPEN-ISSUES.md` T1 |
| E22 | The three bounty components (`my-pledges`, `record-flow`, `streamer-page`) render in the pre-Nerve skin; reapply the restyle on top of the lockdown, contributor from the session only | `OPEN-ISSUES.md`, "THE BOUNTY COMPONENTS LOST THE NERVE SKIN" |
| E23 | **CLOSED 2026-09-16.** `_gate-bounty-claim.mjs` section G now spawns through `startGateServer` (port precheck, readiness poll, stderr kept, nonce); it had reported `0,0,0` twice in a row under build load on the fixed 9 s sleep, and passed 102/0 on the harness immediately after. The other fixed-sleep gates in W5 remain open (E19). | `OPEN-ISSUES.md`, "SECTION G IS A FIXED-SLEEP RACE" |
| E24 | Handles are reassignable by design at the identity layer; every consumer that stores a handle must pin the identity — the durable fix belongs in `identity-store.js` | `OPEN-ISSUES.md`, "A HANDLE IS NOT AN AUTHORIZATION TOKEN" |
| E25 | G4/G5: competing claimants not modelled; MegaChat handles are 3–20 chars against Twitch's 3–25 | `OPEN-ISSUES.md`, "Known gaps still open" (Run A) |
| E26 | Kick OAuth registration validity unproven (a rendering login page proves nothing); needs a full round trip with a real Kick account | `OPEN-ISSUES.md`, P1 (07-29) |
| E27 | The Safari manual fallback for OBS one-click (no loopback mixed-content exemption) is by design; the monitoring toggle applies to the input named *MegaChat Overlay* only | `OPEN-ISSUES.md`, OBS one-click "Open" |
| E28 | Rumble: the harness must read `server_url`/`stream_key` from the live-status API, never env; no Rumble rehearsal harness exists | `OPEN-ISSUES.md`, "Rumble: ingest PROVEN" |
| E29 | `docs/design/copy-bank.md` retired the `/bounty` disclosure; `web/components/bounty/record-flow.tsx` and `web/components/bounty/claim-flow.tsx` still carry sibling preview notices | `docs/ui-overhaul/copy-principles.md`, line 265 |
| E30 | Delete a room from the UI (`deleteRoom` has no caller) | `docs/ui-overhaul/baseline/ideal-paths-local.md` T8 |
| E31 | LiveKit capacity figures remain models, not measurements (L4); no programmatic access to Cloud usage numbers (L2) | `OPEN-ISSUES.md` L2, L4 |
| E32 | `/api/join/mpp` is mounted and gated but unreachable from the join page — retained code or a live mode? ([U1](needs-a-status-call.md)) | `web/lib/join-page.ts`; `server.js` |
| E54 | The 6 h held-clip TTL (`LETTER_HOLD_TTL_MS`) is a guess at a broadcast's length, never measured against a real show | `OPEN-ISSUES.md` P1 (producer mode) |
| E55 | `ready` has no cap of its own; only `GLOBAL_MAX_BYTES` and the hold TTL bound a producer's bench | `OPEN-ISSUES.md` P2 |
| E56 | Restart recovery mid-playback (`playing` → held, never replayed) is gated against a seeded `meta.json`, not against a deploy landing mid-clip | `OPEN-ISSUES.md` P3 |
| E57 | Producer mode was driven through the dashboard with no OBS overlay connected; "Air it" and the overlay-offline banner are asserted from one side only — same gap as row 25a | `OPEN-ISSUES.md` P4 |
| E58 | The join page's suggested guest name is not selected on focus, so a fan who clicks and types appends to it | `OPEN-ISSUES.md` P5 |
| E59 | `_gate-cohost-booth.mjs` unlocks through a form that is no longer mounted and types into the pre-filled `#username`; its arm, re-arm, auto-off and denied sections are uncovered until it is re-pointed | `OPEN-ISSUES.md` B2 (booth picture) |
| E60 | **CLOSED 2026-09-24.** The guest's echo: the booth now plays the guests (the canceller's reference) and the overlay mutes them while it does — `_gate-booth-audio.mjs` 13/0. What remains is E64. | `OPEN-ISSUES.md` E3 |
| E61 | The booth's still-picture watch pauses while its tab is hidden, and reads a deliberate still scene as "still picture" | `OPEN-ISSUES.md` B3, B4 |
| E62 | The guest page's fallback for a booth that predates `mc.picture` (show the host after 2s with no attribute) is reasoned, not gated — no harness can run the old booth against the new join page | `OPEN-ISSUES.md` B6 |
| E63 | A picture that turns into OBS's placeholder MID-session is visible for up to ~1s (`OBS_AFTER` samples) before both sides hold it | `OPEN-ISSUES.md` B7 |
| E64 | The echo fix depends on OBS capturing Desktop Audio on the booth's output device, and whether Chrome's canceller cleanly removes a loud speaker is unmeasured — both need a live session | `OPEN-ISSUES.md` E4 |
| E65 | Live check: does This tab now sound clean to the streamer? The double-copy defect is fixed and gated; nothing recorded the old sound | `OPEN-ISSUES.md` W1 |
| E66 | OBS 31 doubles guests and stingers on stream in either OBS mode — update OBS to 32+ before choosing one (operator) | `OPEN-ISSUES.md` W4 |
| E67 | OBS · speakers gates the streamer's voice for guests under loud playback (10–24% of speech) and adds ~170 ms, unmeasured in conversation | `OPEN-ISSUES.md` W2, W5 |
| E68 | Operator-side: start OBS Virtual Camera; remove or shut down the second "Browser" overlay source; fix the dead "Audio Input Capture" and check Mic/Aux is unmuted | `OPEN-ISSUES.md` W7 |

## Reconciliation note

`OPEN-ISSUES.md` has 76 `###` entries across 16 runs (as of `03aabc0`). Every open bullet found in it maps to a row above or to a `RESOLVED`/`RETRACTED`/`SUPERSEDED` marker in the file itself. Two items in the file are recorded with both an "open" and a later "resolved" form (L1, L5, L6, G9, P6, the Twitch self-capture claim); the resolved form governs and they are omitted here. New in this pass: E1. E38 (2026-09-18): E42–E46 and O15 added; E38, E41, E42, E43 closed. Escrow Session 1 (2026-09-18): E47, O16, O17 added. Session 2 (2026-09-19): E48–E50 added; E47, E49, O17 closed. Identity layer (2026-09-19): E51–E53 and O18 added. MegaChat producer mode (2026-09-19): E54–E58 added; booth picture (2026-09-24): E59–E68 added, E60 closed, B1 closed in the same pass (and its first form superseded — B5); P6 in that run is a gate-diagnostics fix, closed in the same pass and not carried here.
