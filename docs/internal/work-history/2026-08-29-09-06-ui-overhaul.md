# 2026-08-29 → 09-06 — UI overhaul

Sources: `docs/ui-overhaul/plan.md`, `docs/ui-overhaul/audit.md`, `docs/ui-overhaul/copy-principles.md`, `docs/ui-overhaul/design-principles.md`, the `docs/ui-overhaul/baseline/` audits and ideal-path walks, `docs/design/copy-bank.md`, `docs/legacy/README.md`, `DESIGN.md`; commits `7d4b238` → `cd118e7` on `origin/v0-ui-migration`; tags `legacy-ui-2026-08-29`, `ui-v1.5-2026-09-04`. There is no `DECISIONS.md` section for this period; the record is the plan, the audits, the copy bank and the commit messages.

## What shipped

- **A create-room page you can finish** (`7d4b238` "instead of a form you survive", `d3eb5ec` its own page, `7abaebc` cut the explaining, `982cd3a` fixed the defaults, `a99c3d2` capped the width, `9e3c693` the approved hybrid layout, `82b78d3` simpler pricing and recovery controls).
- **The landing** rebuilt around a works-with strip and a closing line ("Stop being a username.", `cb634a1`; then "names the flaw", `0a63d7f`), a real `/account` (`add2380`), the account link, the copy bank (`9018710`).
- **Bounty page**: new headline from the owner's own line — *Your favorite streamer doesn't even know you. Be more than a username.* — and **the preview-build disclosure retired at the owner's call** (`6386a2c`, recorded in the copy bank). Real avatars, the contested/locked bar (`d441caa`); the demo board seeded on boot without an admin key (`c8431b3`, `0e625eb`).
- **Typography unified** on Plus Jakarta Sans with Archivo on the landing hero (`4f2bf02`).
- **BroadcastOS navigation** (`e4c8587`) and the **Nerve system** — film-column hero, pink MEGA mark, near-black skin site-wide (`e92eae7`, 2026-09-04).
- **How-it-works and the landing** lost "the corny furniture" and gained real bounty faces (`ea13e57`).
- **Room entry points unified**, *End room* added (`2a0cd60`); owning a room means landing in it (`3d12a18`); the paused room quieted, the booth follows seats, OBS is the next step (`1254118`); manual OBS setup a peer choice beside Connect OBS (`4a92228`, `4872a70`).
- **`/app` rebuilt as a directory on the Nerve skin** (`cd118e7`).
- **The legacy front end kept, not deleted**: tag `legacy-ui-2026-08-29` is the whole pre-overhaul site; `docs/legacy/README.md` maps what still runs where.

## What it found

- The full UX walk (`docs/ui-overhaul/baseline/ideal-paths.md`): creating a room from the landing snapped back to an empty form with no confirmation, no links, no overlay URL; signed out there was no way back into a room. Fixed across the create-room commits above.
- No delete-room control exists anywhere; `deleteRoom` has no caller in `dashboard-routes.js` (`docs/ui-overhaul/baseline/ideal-paths-local.md`, T8). Still true.
- The copy bank retired the `/bounty` disclosure but two sibling notices still shipped in `web/components/bounty/record-flow.tsx` and `web/components/bounty/claim-flow.tsx` (`docs/ui-overhaul/copy-principles.md`, line 265) — filed there, not resolved.

## Judgment calls

- Re-skin only, never restructure menus — the Nerve system is a visual language over the existing surfaces (`docs/ui-overhaul/plan.md`).
- The disclosure came off `/bounty` because "the page does not launch until settlement is real, so a line saying no funds move describes a state nobody will ever see it in" (`6386a2c`). Pass B Run 1 briefly reversed this by mistake and Run 2 restored the decision ([Pass A and Pass B](2026-09-15-16-pass-a-and-pass-b.md)).
- Nothing deleted: the old site remains reachable and tagged.

## Verification result

`_gate-theme.mjs` was reported red on 2026-08-26 (dark mode rendering a white background, T1) before the Nerve skin landed; whether the near-black skin resolved it has not been re-checked and is [U3](../needs-a-status-call.md). The overhaul's own baseline audits (Lighthouse, axe) are under `docs/ui-overhaul/baseline/`.
