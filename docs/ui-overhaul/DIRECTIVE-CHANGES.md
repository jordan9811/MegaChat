# Directive changes (supersede the run spec)

Standing amendments from Jordan, applied to every phase from here on. Where
these conflict with megachat-ui-overhaul-prompt, THESE WIN.

## 2026-09-02 — Logo directive withdrawn

Jordan has NOT settled on what to do with the logo. Drop it as a constraint:

- The current converged logo (violet→magenta MEGA over white CHAT, chrome
  keyline) is NO LONGER a fixed anchor to design around.
- Do NOT gate palette selection on "does it fight the logo" (R4's logo tests
  are moot).
- Do NOT put the current logo in nav mocks as a settled element, and do NOT
  add it to the Phase 3 PRESERVE list.
- The real logo assets were never modified by this run (wordmark.tsx, the
  public icons, and design-assets/branding/* are all untouched). The only
  logo-related outputs are the palette-against-logo TEST artifacts under
  docs/ui-overhaul/directions/*/ (logo-test*.html/.png, a copied jpg); those
  are to be removed once the Phase 1 research workflow finishes writing there.

## 2026-09-02 — Refocus

The run's emphasis is overall AESTHETIC, UI, and VIBE per section 0 of the
prompt (beautiful, intuitive, fast, exciting; the three-lens balance; the
color-system fix; soften-the-square; copy that grabs). Keep the token/type/
spacing/radius/color and copy research. The logo is simply not part of the
scope until Jordan decides.
